VITE_API_BASE_URL=http://localhost:5000/api/v1
# The get-upload-url Lambda's Function URL — see terraform output
# get_upload_url_function_url after `terraform apply`. Only needed for
# the "Bulk Import" background flow; the regular Import CSV button
# doesn't use this.
VITE_IMPORT_UPLOAD_URL=
